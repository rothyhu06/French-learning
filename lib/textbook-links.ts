export function learningUrl(input:{lessonId:string;sectionId?:string;sourceBlockId?:string}){ const q=new URLSearchParams({mode:"learn"}); if(input.sectionId)q.set("sectionId",input.sectionId); if(input.sourceBlockId)q.set("sourceBlockId",input.sourceBlockId); return `/courses/${input.lessonId}?${q}`; }
export function textbookUrl(input:{lessonId:string;pdfPageIndex:number;sourceBlockId?:string}){ const q=new URLSearchParams({mode:"textbook",pdfPageIndex:String(input.pdfPageIndex)}); if(input.sourceBlockId)q.set("sourceBlockId",input.sourceBlockId); return `/courses/${input.lessonId}?${q}`; }
